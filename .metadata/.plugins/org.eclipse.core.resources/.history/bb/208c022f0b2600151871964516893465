'''
Created on 9 Jul 2015

@author: sduprey
'''

import threading
import thread
import time

exitFlag = 0

class xgdClassifierThread (threading.Thread):
    def __init__(self, threadID, name, Xtrain, ytrain, Xtest):
        threading.Thread.__init__(self)
        self.threadID = threadID
        self.name = name
        self.Xtrain = Xtrain
        self.ytrain = ytrain
        self.Xtest = Xtest
    
    def run(self):
        print "Starting " + self.name
        stepping_through_categories(self.name, self.Xtrain, self.ytrain, self.Xtest)
        print "Exiting " + self.name


def stepping_through_categories(threadName, Xtrain, ytrain, Xtest):


def print_time(threadName, delay, counter):
    while counter:
        if exitFlag:
            thread.exit()
        time.sleep(delay)
        print "%s: %s" % (threadName, time.ctime(time.time()))
        counter -= 1

